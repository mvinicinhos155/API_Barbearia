version: "3.9"

services:
    postgres:
        image: postgres:16
        container_name: api_barbearia
        environment:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: api_barbearia
        ports: 
        - "5432:5432"
